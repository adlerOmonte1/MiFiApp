import { PlaceholderPantalla } from "@/components/placeholder-pantalla";

// 05 · Dashboard — contenido real en A2.6. El tab bar de 4 secciones
// (docs/Navegacion.md §4) todavía no existe; hasta A2.2 esta es la única
// pantalla de la zona privada.
export default function Dashboard() {
  return <PlaceholderPantalla titulo="Dashboard" sprint="A2.6" />;
}
