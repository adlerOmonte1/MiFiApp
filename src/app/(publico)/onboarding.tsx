import { PlaceholderPantalla } from "@/components/placeholder-pantalla";

// 01 · Onboarding — contenido real en A1.6 (docs/ContenidoUI.md §3).
export default function Onboarding() {
  return <PlaceholderPantalla titulo="Onboarding" sprint="A1.6" />;
}
